version: 0.2

env:
  variables:
    JAVA_HOME: "/usr/lib/jvm/java-8-openjdk-amd64"
phases:

  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      
  post_build:
    commands:
      - echo Build completed on `date`
      - mv target/*.war PensionDisbursement-Microservice-0.0.1-SNAPSHOT.war
artifacts:
  files:
    - PensionDisbursement-Microservice-0.0.1-SNAPSHOT.war